import { createContext } from "react";
// const ReactPath = createContext({ reactpath: "http://3.34.2.176" });
const ReactPath = createContext({ reactpath: "http://localhost:3000" });
export default ReactPath;
