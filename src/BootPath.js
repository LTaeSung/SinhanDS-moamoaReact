import { createContext } from "react";
const BootPath = createContext({ bootpath: "http://3.34.136.57:8090" });
// const BootPath = createContext({ bootpath: "http://localhost:8090" });
export default BootPath;
